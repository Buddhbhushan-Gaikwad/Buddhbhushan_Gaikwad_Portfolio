# Buddhbhushan Gaikwad — Portfolio (Frontend)

This is a simple React frontend using Vite.

Setup

1. cd frontend
2. npm install
3. npm run dev

Notes

- The frontend expects a backend at `/api/*`. For local development, run the backend on port 8080 and set a proxy in `vite.config.js` or use a dev proxy.